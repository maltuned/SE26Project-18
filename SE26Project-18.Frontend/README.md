# 前端部分

先 `yarn` 安装依赖，再 `yarn expo start` 启动。

已完成举报、反馈、消息推送、jwt认证用户，等待测试。

使用src/api/api.ts连接后端，在src/api/config.ts填入地址。

设置、评价等待完善，优先级较低。

# 后端部分

在这一块写了，大量 vibe coding，可能存在不规范等内容，但是能跑，当作保底。

dotnet run 启动，注意改数据库访问的密码和数据库初始化。

图片使用minio本地存储，通过url获取。

管理员部分等待完成，做一个简单的前端即可，主要目的是演示管理闭环。