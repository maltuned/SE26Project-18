# 前端部分

先 `yarn` 安装依赖，再 `yarn expo start` 启动。

目前登录点一下会自己填入张三的账密，再点一下即可登录。没做账号和评论功能，主要先把发布、搜索、回应招募和聊天做了，但是不一定可靠。

后端使用src/api/backend-sim.ts模拟，与实际后端适配时要注意提供data-patterns内的数据，在api.ts中解析。数据模板调整就要搜索前端代码一起调了。

临时数据放在data.json，由ai直接生成，存在实际操作不会出现的逻辑漏洞。