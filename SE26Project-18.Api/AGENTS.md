# C# Style Rules

- Add a blank line between consecutive class members, including fields, constants, properties, constructors, and methods.
- Always use braces for control-flow statements such as `if`, `else`, `for`, `foreach`, and `while`, even when the body contains only one statement.
